name: include
data:
  a:
    id: 1
test:
  - name: include 1
    data: a
    sentences: []
    errors: 1
    expression: __Orders.filter(p=>(p.id===id)).include(p=>p.customer)
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>p.customer) error: SchemaError:
      entity __Orders not found
  - name: include 2
    data: a
    sentences: []
    errors: 1
    expression: __Orders.filter(p=>(p.id===id)).include(p=>p.details)
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>p.details) error: SchemaError:
      entity __Orders not found
  - name: include 3
    data: a
    sentences: []
    errors: 1
    expression: __Orders.filter(p=>(p.id===id)).include(p=>[p.details,p.customer])
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details,p.customer]) error:
      SchemaError: entity __Orders not found
  - name: include 4
    data: a
    sentences: []
    errors: 1
    expression: >-
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.include(q=>q.product),p.customer])
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.include(q=>q.product),p.customer])
      error: SchemaError: entity __Orders not found
  - name: include 5
    data: a
    sentences: []
    errors: 1
    expression: >-
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.include(q=>q.product.include(p=>p.category)),p.customer])
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.include(q=>q.product.include(p=>p.category)),p.customer])
      error: SchemaError: entity __Orders not found
  - name: include 6
    data: a
    sentences: []
    errors: 1
    expression: >-
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.map(p=>{quantity:p.quantity,unitPrice:p.unitPrice,productId:p.productId}),p.customer])
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.map(p=>{quantity:p.quantity,unitPrice:p.unitPrice,productId:p.productId}),p.customer])
      error: SchemaError: entity __Orders not found
  - name: include 7
    data: a
    sentences: []
    errors: 1
    expression: >-
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.include(q=>q.product).map(p=>{quantity:p.quantity,unitPrice:p.unitPrice,productId:p.productId}),p.customer])
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>[p.details.include(q=>q.product).map(p=>{quantity:p.quantity,unitPrice:p.unitPrice,productId:p.productId}),p.customer])
      error: SchemaError: entity __Orders not found
  - name: include 8
    data: a
    sentences: []
    errors: 1
    expression: >-
      __Orders.filter(p=>(p.id===id)).include(p=>[p.customer.map(p=>p.name),p.details.include(p=>p.product.include(p=>p.category.map(p=>p.name)).map(p=>p.name)).map(p=>[p.quantity,p.unitPrice])])
    error: >-
      SintaxisError: complete expression:
      __Orders.filter(p=>(p.id===id)).include(p=>[p.customer.map(p=>p.name),p.details.include(p=>p.product.include(p=>p.category.map(p=>p.name)).map(p=>p.name)).map(p=>[p.quantity,p.unitPrice])])
      error: SchemaError: entity __Orders not found
errors: 8
